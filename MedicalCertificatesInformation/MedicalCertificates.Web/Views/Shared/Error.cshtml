@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Ошибка.</h1>
<h2 class="text-danger">Во время обрабоки вашего запроса произошла ошибка.</h2>

@*@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}*@
@if (Model.MessageDescription != null)
{
    <p class="text-danger">@Model.MessageDescription</p>
}